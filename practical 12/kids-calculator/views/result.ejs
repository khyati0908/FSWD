<!DOCTYPE html>
<html>
<head>
  <title>Result</title>
  <link rel="stylesheet" href="/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="fun-bg">

  <div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="calculator-card p-5 text-center shadow-lg">
      <h1 class="rainbow-text">🎉 Result 🎉</h1>
      <h2 class="display-5 mt-3">
        <span class="num"><%= num1 %></span>
        <% if (operation === "add") { %> ➕ <% } %>
        <% if (operation === "subtract") { %> ➖ <% } %>
        <% if (operation === "multiply") { %> ✖ <% } %>
        <% if (operation === "divide") { %> ➗ <% } %>
        <span class="num"><%= num2 %></span>
        = <span class="final-result"><%= result %></span>
      </h2>
      <a href="/" class="btn btn-lg mt-4 rounded-pill back-btn">🔙 Try Again</a>
    </div>
  </div>

</body>
</html>
