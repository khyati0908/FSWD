<!DOCTYPE html>
<html>
<head>
  <title>Total Income Result</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(120deg, #89f7fe, #66a6ff);
      font-family: "Poppins", sans-serif;
    }
    .card {
      border-radius: 25px;
      border: 3px solid #28a745;
    }
    .gradient-text {
      font-size: 2.2rem;
      font-weight: bold;
      background: linear-gradient(90deg, #ff416c, #ff4b2b);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    .income-text {
      font-size: 1.3rem;
    }
    .total {
      font-size: 2rem;
      font-weight: bold;
      color: #28a745;
      animation: pop 0.8s ease;
    }
    @keyframes pop {
      0% { transform: scale(0.5); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }
    .btn-back {
      margin-top: 20px;
      background: #ffcc00;
      border-radius: 15px;
      padding: 10px 20px;
      font-weight: bold;
      border: none;
    }
    .btn-back:hover {
      background: #ff9800;
      color: #fff;
    }
  </style>
</head>
<body>

  <div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="card p-5 shadow-lg text-center" style="max-width:500px; width:100%;">
      <h1 class="gradient-text">📊 Income Summary</h1>
      <p class="income-text">Source 1 Income: ₹<%= source1 %></p>
      <p class="income-text">Source 2 Income: ₹<%= source2 %></p>
      <hr>
      <h2 class="total">💵 Total Income: ₹<%= totalIncome %></h2>
      <a href="/" class="btn btn-back">🔙 Back</a>
    </div>
  </div>

</body>
</html>
