<!DOCTYPE html>
<html>
<head>
  <title>Tax Income Form</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #f6d365, #fda085, #a1c4fd, #c2e9fb);
      background-size: 400% 400%;
      animation: gradientMove 12s ease infinite;
      font-family: "Poppins", sans-serif;
    }
    @keyframes gradientMove {
      0% {background-position: 0% 50%;}
      50% {background-position: 100% 50%;}
      100% {background-position: 0% 50%;}
    }
    .card {
      border-radius: 25px;
      border: 3px solid #ffcc00;
    }
    .gradient-text {
      font-size: 2rem;
      font-weight: bold;
      background: linear-gradient(90deg, #ff6a00, #ee0979, #00c6ff, #0072ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    .input-field {
      border-radius: 15px;
      border: 2px solid #0072ff;
      padding: 12px;
      text-align: center;
      font-size: 1.2rem;
    }
    .btn-custom {
      background: linear-gradient(90deg, #00c6ff, #0072ff);
      border: none;
      border-radius: 15px;
      padding: 12px;
      font-size: 1.2rem;
      color: #fff;
    }
    .btn-custom:hover {
      background: linear-gradient(90deg, #ff6a00, #ee0979);
    }
  </style>
</head>
<body>

  <div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="card p-5 shadow-lg" style="max-width:500px; width:100%;">
      <h1 class="gradient-text text-center mb-3">💼 Tax Form</h1>
      <p class="text-center text-muted">Enter your incomes from two sources below</p>

      <% if (error) { %>
        <div class="alert alert-danger text-center"><%= error %></div>
      <% } %>

      <form action="/calculate" method="POST">
        <div class="mb-3">
          <input type="text" class="form-control input-field" name="source1" placeholder="Source 1 Income">
        </div>
        <div class="mb-3">
          <input type="text" class="form-control input-field" name="source2" placeholder="Source 2 Income">
        </div>
        <button class="btn btn-custom w-100">💰 Calculate Total</button>
      </form>
    </div>
  </div>

</body>
</html>
